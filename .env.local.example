NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Sprint 13: server-only food photo provider. No default model is selected.
# Real image QA is mandatory before enabling/merging. Never prefix secrets NEXT_PUBLIC_.
OPENAI_API_KEY=
AI_FOOD_MODEL=
SUPABASE_SERVICE_ROLE_KEY=
# Local loopback Supabase only; ignored on Vercel. Mock results are visibly labeled.
AI_FOOD_LOCAL_MOCK=false
# Protected cleanup endpoint; keep unset until scheduling is explicitly approved.
FOOD_PHOTO_CLEANUP_SECRET=
# See docs/qa/sprint-13-retention.md for opt-in pg_cron/pg_net setup.
# No staging/Production scheduler is enabled by migrations or deployment.

# Server-only weekly summary model; uses the same secure OpenAI key.
AI_WEEKLY_MODEL=
