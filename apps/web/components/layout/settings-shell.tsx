import Link from 'next/link';
import type { ReactNode } from 'react';
import { AppShell } from './app-shell';

export function SettingsShell({ active, children }: { active: 'profile' | 'goals'; children: ReactNode }) {
  return <AppShell active="settings"><div className="grid gap-7"><header><p className="text-xs font-bold uppercase tracking-[0.18em] text-primary">Account</p><h1 className="mt-2 text-3xl font-bold tracking-[-0.035em] sm:text-4xl">Settings</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Keep your identity and planning targets up to date.</p></header><div className="grid gap-7 lg:grid-cols-[190px_minmax(0,1fr)]"><nav aria-label="Settings" className="flex gap-2 overflow-x-auto lg:grid lg:content-start"><Link href="/settings/profile" aria-current={active === 'profile' ? 'page' : undefined} className={`shrink-0 rounded-lg px-4 py-2.5 text-sm font-semibold transition ${active === 'profile' ? 'bg-secondary text-white' : 'text-muted-foreground hover:bg-muted hover:text-foreground'}`}>Profile</Link><Link href="/settings/goals" aria-current={active === 'goals' ? 'page' : undefined} className={`shrink-0 rounded-lg px-4 py-2.5 text-sm font-semibold transition ${active === 'goals' ? 'bg-secondary text-white' : 'text-muted-foreground hover:bg-muted hover:text-foreground'}`}>Goals & targets</Link></nav><div className="min-w-0">{children}</div></div></div></AppShell>;
}
