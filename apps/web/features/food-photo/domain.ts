import type { FoodMacroEstimate,FoodPhotoItem } from '@tfk/types';
export function photoTotals(items:FoodPhotoItem[]):FoodMacroEstimate{return items.reduce((n,i)=>({calories:Number((n.calories+i.estimated_calories).toFixed(2)),protein_g:Number((n.protein_g+i.protein_g).toFixed(2)),carbs_g:Number((n.carbs_g+i.carbs_g).toFixed(2)),fat_g:Number((n.fat_g+i.fat_g).toFixed(2))}),{calories:0,protein_g:0,carbs_g:0,fat_g:0});}
export function resizePortion(item:FoodPhotoItem,amount:number):FoodPhotoItem{const ratio=amount/item.estimated_portion.amount;return {...item,estimated_portion:{...item.estimated_portion,amount},estimated_calories:Number((item.estimated_calories*ratio).toFixed(2)),protein_g:Number((item.protein_g*ratio).toFixed(2)),carbs_g:Number((item.carbs_g*ratio).toFixed(2)),fat_g:Number((item.fat_g*ratio).toFixed(2))};}
export function confidenceText(confidence:number){return confidence>=0.8?'High confidence':confidence>=0.5?'Medium confidence':'Low confidence — check this item carefully';}
export function localPhotoTime(iso:string,timeZone:string){const parts=new Intl.DateTimeFormat('en-CA',{timeZone,year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(new Date(iso));const p=(key:string)=>parts.find(x=>x.type===key)?.value;return `${p('year')}-${p('month')}-${p('day')}T${p('hour')}:${p('minute')}`;}
